<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - WhatsApp Web API</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background-color: #f8f9fa;
    }
    .error-container {
      max-width: 500px;
      padding: 2rem;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
      text-align: center;
    }
    .error-icon {
      font-size: 5rem;
      color: #dc3545;
      margin-bottom: 1rem;
    }
    .error-status {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-icon">⚠️</div>
    <div class="error-status">
      <% if (error && error.status) { %>
        <%= error.status %>
      <% } else { %>
        Error
      <% } %>
    </div>
    <h1 class="h4 mb-3"><%= message %></h1>
    
    <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
      <div class="alert alert-danger mt-4 text-start">
        <pre><%= error.stack %></pre>
      </div>
    <% } %>
    
    <div class="mt-4">
      <p class="mb-3">Beberapa rute yang tersedia:</p>
      <ul class="list-group mb-3 text-start">
        <li class="list-group-item"><a href="/login">Login</a> - Halaman login</li>
        <li class="list-group-item"><a href="/register">Register</a> - Halaman registrasi</li>
        <li class="list-group-item"><a href="/">Homepage</a> - Halaman utama (akan diarahkan ke login jika belum masuk)</li>
      </ul>
      <a href="/" class="btn btn-primary">Kembali ke Halaman Utama</a>
    </div>
  </div>
</body>
</html>